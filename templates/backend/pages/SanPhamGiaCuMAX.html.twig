{# Kế thừa layout backend #}
{% extends "backend/layouts/layout.html.twig" %}

{# Nội dung trong block title #}
{% block title %}
Bảng tin
{% endblock %}
{# End Nội dung trong block title #}

{# Nội dung trong block headline #}
{% block headline %}
Bảng tin
{% endblock %}
{# End Nội dung trong block headline #}

{# Nội dung trong block content #}
{% block content %}
<h2 class="text-center">Sách cũ có giá <br> <b> cao nhất</b></h2>
<div class="container">
    <div class="row">
        <div class="col-md-12">
            <table class="table table-bordered table-hover mt-2">
                <thead class="thead-light">
                    <tr>
                        <th>Tên sách</th>
                        <th>Giá sách</th>
                    </tr>
                </thead>
                <tbody>
                    {% for SC_GMAX in SC_GMAX %}
                    <tr>
                        <td>{{ SC_GMAX.TENSACH }}</td>
                        <td>{{ SC_GMAX.GIAMUA }}</td>

                    </tr>
                    {% endfor %}
                </tbody>
            </table>
            <a href="dashboard.php" class="btn btn-info">Trở về</a>

        </div>
    </div>

</div>

{% endblock %}
{# End Nội dung trong block content #}